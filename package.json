{
  "name": "3cx-analytics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static files already built'",
    "vercel-build": "echo 'Static files already built'"
  },
  "dependencies": {
    "mongodb": "^6.18.0",
    "bcrypt": "^6.0.0"
  },
  "engines": {
    "node": "22.x",
    "npm": ">=10.0.0"
  }
}
